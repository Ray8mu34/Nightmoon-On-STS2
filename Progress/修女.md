# 修女当前进度

## 当前阶段

- 角色、祷告系统、遗物池、药水池和大部分卡牌已经实现并经过多轮测试。
- 当前处于收尾阶段，工作重点不再是大面积做新内容，而是清理残余机制问题、显示问题和少量剩余单卡问题。
- `Progress/卡牌测试结果.csv` 仍然是逐卡测试主表；本文件只保留“现在的状态”，不再保留批次流水账。

## 当前未修复

### 共享显示问题

- `打击`
  - 实际已经能吃到力量，但卡面数字不会动态变化。
- `防御`
  - 实际已经能吃到敏捷，但卡面数字不会动态变化。
- 备注
  - 这不是只有 `打击` / `防御` 两张的问题，而是同类伤害牌 / 格挡牌都可能受影响。
  - 当前已确认是显示层问题，不是基础结算问题。

### 单卡未修复

- `天使庇护`
  - 复制品确实进入了抽牌堆，也占了位置，但抽牌堆数字没有 `+1`。
- `神圣庇护`
  - 卡面仍显示占位符 `{Buffer}`，文本未正确替换。

## 尝试修复但失败

- `天使庇护`
  - 失败尝试：改成“创建一张新的合法复制品，再加入当前战斗中的抽牌堆”。
  - 失败表现：复制品确实进了抽牌堆，但抽牌堆计数仍然不更新。
  - 当前判断：这类“复制牌洗入抽牌堆”应该优先查官方现成 API，不能继续靠手搓克隆和入堆流程猜实现。

- `神圣庇护`
  - 失败尝试：曾按“源码和本地化已经一致，可能是旧包误测”的方向判断。
  - 失败表现：用户复测后，卡面依旧显示 `{Buffer}` 占位符。
  - 当前判断：这不是旧包问题，而是实际文本替换仍然有问题。

- `打击` / `防御` 对应的动态卡面显示
  - 失败尝试：加过 `CardDynamicVarRefreshPatch`，想在 power 变化后强制刷新可见卡牌。
  - 失败表现：用户复测后，卡面数字依然不动态变化，也没有按高于/低于基础值正确变色。
  - 当前判断：实际伤害 / 格挡结算链路已基本修正，但显示层刷新方案还没有找到真正有效的入口。

## 当前待用户验证

- `正义天平`
  - 已实现为 ancient 能力牌，基础 3 费，升级改 2 费。
  - 当前实现参考 `清算时刻` 的伤害来源与 `恶魔刀刃` 的触发形式：
    - 回合开始时
    - 读取上回合总伤害
    - 对随机敌人造成其中 `20%` 的伤害
  - 已补齐卡牌与 power 的中英文本地化，但还没有经过用户实机复测。
- `沐浴圣水`
  - 已按最新要求改成处理战斗内可见牌堆。
  - 当前实现会把抽牌堆、弃牌堆和手牌中的诅咒送入消耗堆。
  - 还没有经过用户实机复测。
- `心灵绽放`
  - 机制没有继续改动，仍保持“祷告牌额外打出一次”的现状。
  - 卡面描述已按最新要求改成“额外打出一次”，不再显示“获得重放 1”。
  - 还没有经过用户实机复测。

## 下一步优先级

1. 先等用户复测 `正义天平`。
2. 如果 `正义天平` 通过，下一优先级是：
   - `神圣庇护`
   - `天使庇护`
   - `打击` / `防御`
3. `天使庇护` 暂时不要继续硬修，优先等查到官方“复制牌洗入抽牌堆”的现成实现后再动。
